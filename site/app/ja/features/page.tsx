import FeaturesPage from "../../features/page";
import { pageMetadata } from "../../seo";
export const metadata = pageMetadata("ja", "features", "/features");
export default function Page() { return <FeaturesPage locale="ja" />; }
